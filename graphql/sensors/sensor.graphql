extend type Query {
    sensor(serial_number: String! @eq): Sensor @find
}

type Sensor {
    id: ID!
    name: String!
    serial_number: String!
    latitude: Float!
    longitude: Float!
    batteryLevel: Int!
    status: Boolean
    measures: [Measurement!] @hasMany
}
